#include "monty.h"

/**
 * f_stack - this function prints the top
 * @head: the stack head node
 * @counter: the line number
 *
 * Return: Nothing
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
