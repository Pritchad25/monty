#include "monty.h"

/**
 * f_nop - the function to be implemented.
 * @head: the head node of the stack
 * @counter: the line number.
 *
 * Return: Nothing
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void)counter;
	(void)head;
}
